{
  "name": "@cognis-digital/mcpscan",
  "version": "0.1.0",
  "description": "Minimal TypeScript port of mcpscan's passive MCP tools/list check",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test \"dist/test/*.test.js\""
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
